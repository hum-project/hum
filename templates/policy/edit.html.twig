{% extends 'base.html.twig' %}

{% block title %}Edit Policy{% endblock %}

{% block body %}
    <h1>Edit a Policy</h1>
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.text) }}
    {{ form_row(form.source) }}
    {{ form_row(form.policyTheme) }}
    {{ form_row(form.argument) }}
    {{ form_row(form.language) }}
    {{ form_end(form) }}
    <a class="button" href="{{ path('vote_add_policy', {policy: policy.id}) }}" >Add vote</a>
{% endblock %}
