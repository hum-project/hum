{% extends 'base.html.twig' %}

{% block title %}Hum - news{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>{{ title }}</h1>
    <time>{{ publish_date|date("d/m/Y") }}</time>
    <div>{{ text|raw }}</div>
</div>
{% endblock %}
